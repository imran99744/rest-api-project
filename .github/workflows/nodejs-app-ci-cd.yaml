name: Node.js REST API CI/CD

on:
  release:
    types: [created]

jobs:
  build_and_push:
    runs-on: ubuntu-latest

    env:
      DOCKER_HUB_USERNAME: ${{ secrets.DOCKER_HUB_USERNAME }}
      DOCKER_HUB_PASSWORD: ${{ secrets.DOCKER_HUB_PASSWORD }}
      DOCKER_HUB_REGISTRY: ${{ secrets.DOCKER_HUB_REGISTRY }}
      WEATHER_API_KEY: ${{ secrets.WEATHER_API_KEY }}

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "14"

      - name: Install Dependencies
        run: npm install

      - name: Get Release Version
        id: get_release_version
        run: |
          echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Build Docker Image
        run: |
          docker build \
            -t "${{ env.DOCKER_HUB_REGISTRY }}/${{ env.DOCKER_HUB_USERNAME }}/nodejs-api:${{ steps.get_release_version.outputs.version }}" \
            .

      - name: Log in to Docker Hub
        run: |
          echo "${{ env.DOCKER_HUB_PASSWORD }}" \
            | docker login -u "${{ env.DOCKER_HUB_USERNAME }}" --password-stdin "${{ env.DOCKER_HUB_REGISTRY }}"

      - name: Push Docker Image
        run: |
          docker push "${{ env.DOCKER_HUB_REGISTRY }}/${{ env.DOCKER_HUB_USERNAME }}/nodejs-api:${{ steps.get_release_version.outputs.version }}"

      - name: Echo success
        run: |
          IMAGE_TAG=${{ steps.get_release_version.outputs.version }}
          echo "✅ Pushed image: ${{ env.DOCKER_HUB_REGISTRY }}/${{ env.DOCKER_HUB_USERNAME }}/nodejs-api:${IMAGE_TAG}"

      - name: Update K8s manifest with new tag
        run: |
          IMAGE_TAG=${{ steps.get_release_version.outputs.version }}
          sed -i \
            's|^\(\s*image:\s*\).*|\1'"${{ env.DOCKER_HUB_REGISTRY }}/${{ env.DOCKER_HUB_USERNAME }}/nodejs-api:${IMAGE_TAG}"'|' \
            terraform-eks/k8s/deployment.yaml

          echo "--- Updated terraform-eks/k8s/deployment.yaml ---"
          cat terraform-eks/k8s/deployment.yaml

      # ----------------------------------------------------------------
      # Continuous-Deployment steps (comment out for now; uncomment later)
      # ----------------------------------------------------------------

      # - name: Install AWS CLI
      #   run: |
      #     sudo apt-get update
      #     sudo apt-get install -y awscli

      # - name: Configure AWS CLI
      #   run: |
      #     aws configure set aws_access_key_id     ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     aws configure set aws_secret_access_key ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #     aws configure set default.region        us-east-1

      # - name: Deploy to EKS
      #   run: |
      #     aws eks update-kubeconfig --name my-eks-cluster --region us-east-1
      #     kubectl apply -f terraform-eks/k8s/deployment.yaml
      #     kubectl apply -f terraform-eks/k8s/service.yaml

      # - name: Restart Deployment
      #   run: |
      #     kubectl rollout restart deployment nodejs-api

      # - name: Verify Deployment
      #   run: |
      #     kubectl get pods --namespace default
